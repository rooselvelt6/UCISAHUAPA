<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="egreso.list">
      <field name="name">egreso list</field>
      <field name="model">egreso.egreso</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="egreso.action_window">
      <field name="name">egreso window</field>
      <field name="res_model">egreso.egreso</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="egreso.action_server">
      <field name="name">egreso server</field>
      <field name="model_id" ref="model_egreso_egreso"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="egreso" id="egreso.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="egreso.menu_1" parent="egreso.menu_root"/>
    <menuitem name="Menu 2" id="egreso.menu_2" parent="egreso.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="egreso.menu_1_list" parent="egreso.menu_1"
              action="egreso.action_window"/>
    <menuitem name="Server to list" id="egreso" parent="egreso.menu_2"
              action="egreso.action_server"/>
    -->
  </data>
</odoo>